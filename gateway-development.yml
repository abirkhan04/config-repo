server:
  port: 9000

security:
  basic:
    enabled: false

ribbon:
  ReadTimeout: 10000
  eureka:
    enabled: false

zuul:
  routes:
    chartofaccounts:
      sensitiveHeaders: Cookie,Set-Cookie
      url: http://localhost:8081